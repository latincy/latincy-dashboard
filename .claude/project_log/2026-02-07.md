# 2026-02-07 Session Log

## Branch: `add-new-dashboard-demos` (not merged)

## Work Done
- Implemented 4 new Streamlit dashboard demos:
  1. **NER Highlighter** (`pages/4_ner_demo.py`) — entity highlighting with PER/LOC/NORP legend
  2. **Dependency Tree Visualizer** (`pages/5_dependency_demo.py`) — per-sentence parse trees with compact mode
  3. **Word Similarity Explorer** (`pages/6_similarity_demo.py`) — two-tab interface with floret-compatible manual similarity over DCC vocabulary candidates
  4. **Morphology Explorer** (`pages/7_morphology_demo.py`) — token table + detail view with human-readable morph features
- Updated `app.py` home page with links to all 4 new demos
- Code-reviewed and fixed: duplicate dict key bug in morph labels, duplicate token selector, dead code removal
- Set up local `.venv` with `uv` for testing

## Pending
- **Local testing incomplete** — Streamlit pages showed perpetual "running" spinner, never loaded content. Server was healthy (healthz OK, HTML served). Likely a browser/WebSocket issue. Needs investigation next session.
- Branch NOT merged to main — merge after local verification passes
- No new dependencies required (all demos use existing packages)
